# Poke — decision record

Append only. When a decision changes, add a new row and set the old row's status to
`Superseded by #N`. Never edit or delete an old row: it was true when it was made, and the
reason behind it is the part you cannot rebuild later.

Read this file before you change or undo an architectural choice. If you are about to do
something this file argued against, the burden is on you to say what changed.

Dated narrative does not belong here. Run logs live in `docs/` with a date in the filename.
See `docs/rebuild-log-2026-08-06.md`.

## Decisions

Rows 1–8 come from the original run log. Rows 9–13 were reconstructed from the shipped
artifacts and commit history on 7 August 2026, so their stated reasons are inferred, not
quoted. Correct any row that is wrong — reconstruction is the one case where editing an old
row is allowed, because the row was never a first-hand record.

| # | Decision | Why | Status |
|---|----------|-----|--------|
| 1 | Name the app **Poke** ("Poke: Peptide & GLP-1 Tracker") | Short, friendly, literal — an injection is a poke. No collision in the niche: the iTunes API shows "poke tracker" returns only Pokemon apps. Dosely and Pip are weaker. Keeps the ASO keywords in the subtitle, like every winner in the category. | Superseded by #9 |
| 2 | Keep the bundle id `industries.peptide.tracker` | Ships as an update to the existing App Store record `6764757185`. Only the display name changes. Changing it would mean a new app and the loss of all reviews. | Live |
| 3 | Keep the domain layer (pk, reconstitution, rotation, scheduling, units). Replace the UI layer. | The domain math is the app's hard-won value. The complaint was about UX, not correctness. Smallest change that solves the problem. | Live |
| 4 | One migration (v5) adding `side_effect_logs`. Onboarding reuses the existing tables. | The schema already models meds, injections, measurements and preferences well. A side effect is its own shape, not a kind of measurement. | Live |
| 5 | Cut food and calorie tracking | It is the biggest feature in the app we studied, and it is weeks of work: a food database plus search. Water and protein quick-log through `measurements.kind` is the cheap adjacent win. | Live |
| 6 | Cut progress photos | Camera and photo storage are too heavy for the time available. The schema can take them later without a migration conflict. | Live |
| 7 | Work on branch `rebuild/poke`, with a WIP snapshot committed first | Reversible. `master` stays untouched until the rebuild is proven. | Live |
| 8 | Verify on expo web first, iOS simulator last | Web preview renders the real app and iterates in seconds. The simulator is the shipping artifact, so it gets the final pass. | Live |
| 9 | Rename the listing to **Poke: Peptide & GLP-1 Log** | "Tracker" is the most contested word in the category. "Log" is passive and matches the medical positioning: the app records what the user types, it does not track or infer anything. Lower App Review risk under guideline 1.4.1. | Live |
| 10 | Subscription: monthly $9.99, yearly **$39.99** with a **3-day** free trial. No trial on monthly. No discount ladder. | $39.99 matches Shotsy's cheapest tested point and beats Peps at $44.99; it also makes the badge read "Save 67%" instead of "Save 58%". The trial was one month through 1.4.0 prep; owner cut it to 3 days (Aug 2026) to match the category (Pep AI 3 days, Shotsy 7) and pull revenue forward. The counter-data stays on record — 17–32-day trials convert 42.5% vs 25.5% for ≤4 days, and a weekly injector logs ≤1 shot in 3 days — so trial length is the first post-launch A/B test. No trial on monthly, because a free month on $9.99 gives the plan away. No discounts, because ~90% of the category sells at full price. Full working: `docs/market.md`. | Live |
| 11 | Gate the paid features with RevenueCat, and unlock everything if the store is unreachable | A reviewer or an offline user must never meet a door that will not open. Failing open costs a few unpaid unlocks. Failing closed costs a rejection. | Live |
| 12 | No account, no sign-in, no server. All data stays on the device. | It is the strongest privacy claim in the category, and it removes the single most common paid-app rejection: a reviewer who cannot get in. | Live |
| 13 | The App Store listing is code (`store.config.json`, pushed with `eas metadata:push`) | A hand edit in App Store Connect is invisible to review and is lost on the next push. The listing wording is medical-risk surface, so it belongs under version control. | Live |
| 14 | Agent documentation uses four layers, with append-only decisions | Always-loaded context is a fixed budget, so detail moves down the tree instead of being deleted. See `../docs/agent-docs.md`. | Live |
| 15 | Ask for a rating from five one-time triggers, with no calendar floor, a 10-day cooldown, and a permanent "Rate Poke" row in Profile | StoreKit allows 3 sheets per 365 days, can show nothing, and never says which happened. So the budget is small and must be spent while the app is new. The old gate counted shots and days since install, which pushed the first ask past day 90 — at 0 ratings that is a wasted quarter. Value delivered is the floor instead, which is also what the HIG describes ("after they complete a task"). Each trigger fires once, so the three asks come from three different wins: a reconstitution answer, a second logged shot, a level curve with 3+ doses, an export, or a 4-week streak. Three asks then land inside 30 days. The manual row uses `?action=write-review`, never `requestReview()`, because Apple says not to call that API from a button — and it is the only way back for a user the system skipped in silence. No pre-prompt and no gating: guideline 5.6.1 disallows custom review prompts, and Section 3 forbids filtered feedback. Evidence: Shotsy has 1★ reviews for asking on every shot; Peptide Tracker sits at 4.93★ with no prompt complaints. Policy is pure in `src/services/reviewGate.ts` with a test. | Live |
| 16 | Every preset half-life carries a citation. Presets are tiered `label` / `trial` / `unsourced`, and an `unsourced` preset gets no half-life, no Tmax and no level curve. | An invented half-life is worse than a missing peptide, because the level curve then states a number that no source supports, and the curve is the thing users trust. The shipped catalog had BPC-157 at 4h and TB-500 at 48h with no human source: BPC-157 animal data says under 30 minutes, so the app was drawing a curve about 8x too long. Both are now `unsourced` and the app says so on the schedule screen and on the plan card. The tier is a discriminated union in `src/domain/peptides.ts`, so an uncited half-life is a type error and not a review comment. Hexarelin, GHRP-2, SS-31, MOTS-c, IGF-1 LR3, AOD-9604, melanotan II, kisspeptin, glutathione and 5-Amino-1MQ were researched and left out, because the circulating figures trace to rat studies or to vendor pages that disagree. Semax and Selank are intranasal and the route model is sc/im only. | Live |
| 17 | One schedule screen per selected medication, at `/onboarding/schedule/[index]`, instead of one dose question for a primary medication | The old flow asked for a dose once and applied it to the first selection, so a user on two peptides finished onboarding with one real schedule and one guess. Poke's whole payoff is the level curve and the shot-day reminder, and both need a real dose per medication. The cost is one extra screen per extra medication, paid only by users who select more than one, and the step counter adapts (`onboardingTotalSteps`). Stacking every dose on one screen was rejected: it makes the common single-medication case worse to serve the rare case. | Live |
| 18 | The last onboarding screen is a computed plan card, with a short build beat before it. No goal date, no pace, no outcome. | The screen was a summary table that repeated the user's own answers, so it read as a form receipt and not as a payoff. It now shows what only Poke can work out: the next shot date with a countdown, the first four weeks of the level curve with the settled week named, and the first four rotation sites. Every number is arithmetic on what the user typed plus a cited half-life, and the math lives in `src/services/onboardingPlan.ts` so the screen only draws it. The Cal-AI pattern this borrows from ends on a projected goal date. Poke does not do that: `store-setup.md` §1A keeps the app clear of advice, diagnosis, treatment and dose recommendation, and a date is a prediction about a body. The build beat names real work, runs about 1.4 seconds, and is skipped under Reduce Motion. The curve samples at four points per half-life, because a fixed step count aliases a short half-life and draws peak heights that come from the sample grid and not from the drug. A peptide that returns near zero between doses is described as clearing, never as steady. | Superseded by #20 |
| 19 | Onboarding does not enforce `FREE_MEDICATION_LIMIT`, so a free user can seed more than one medication | The limit is enforced where a medication is added later (`app/medications/new.tsx`, `app/medications/index.tsx`). Onboarding runs once per install, so the leak is bounded by one pass. Blocking a user's real routine on the first run is the worse trade: it makes the app wrong about the user on day one, and it puts a paywall in front of the setup that the paywall is supposed to sell. Revisit if the free tier is ever the main revenue risk. | Live |
| 20 | The onboarding reveal **does** end on a projected goal date, with a live pace slider, after a 13.8 second build beat. Reverses row 18's "no goal date, no pace, no outcome" and its "about 1.4 seconds". | Owner's instruction, 8 Aug 2026: "I'm OK with payoff moments and copying the whole persuasion and positioning and moments and payoff of MeAgain", and "30 / 14 with the simulated progress too". Row 18's ban was mine, not the owner's, and it was overruled deliberately. The concern behind row 18 stands and was stated once before building: a date about a body is the pattern `store-setup.md` §1A exists to avoid. What makes the shipped version defensible is that it never hides where the number comes from. The date is `(current − goal) ÷ weekly pace` and nothing else: no model, no cohort, no regression. `planProjection` in `src/services/onboardingPlan.ts` takes only the weights, the pace and a `now` frozen at mount, so the arithmetic is checkable by hand. The card prints the sum in words under the date, the pace slider recomputes it live so a user can watch the date move with its own input, and the hedge names it as arithmetic and points at the clinician. Past `MAX_PROJECTION_WEEKS` (260) the card says "Over five years" and refuses a date rather than printing one nobody should plan against, and it stays mounted while it says so, because unmounting the card would take the slider out from under the user's finger. With no weight or no goal weight the projection is skipped entirely and the plan shows the shot schedule alone, per row 18's surviving rule against invented numbers. The 13.8 s beat is the recording's own measured duration, interpolated through four measured points rather than eased by guess, and Reduce Motion still skips it. Also corrects the parenthetical in row 17: the step counter does **not** adapt. It is a fixed 23, and the per-medication schedule run divides one counted step between its screens, because a growing denominator ran the bar backwards under the user's thumb. | Live |
| 21 | An all-clear day is a `side_effect_logs` row with the dedicated effect kind `all_clear`, not a new table and not a new column | The day-after check-in was designed to offer "mark the day clear", and the shipped copy had to retreat to "log how you feel" because no record of a clear day existed. The record lives in the same table as a symptom because every reader of the side-effect timeline — the check-in scheduler's "answered" check, the Effects chart, History, the CSV export — needs clears and symptoms in one ordered stream, and the scheduler then treats an all-clear as answering the window with no change at all. No migration: `effect` is TEXT, and a build older than the control parses `all_clear` softly as a custom effect named "All clear". Severity 0 fills the NOT NULL column; the kind, not the number, says there was nothing to measure, so every reader that would print "0 of 10" must branch on the kind instead. One clear per local day, enforced in `markDayAllClear` by returning the existing row, because "the day is clear" is a fact about the day and a duplicate would draw two marks. A symptom logged on the same day stands beside a clear rather than replacing it: both are things the user said, and the record keeps both. On the chart a clear is a hollow violet ring on the axis baseline — an answered zero, distinct from the dashed line of a day nobody asked about. | Live |
| 22 | The paywall's hero is the user's own estimated level curve, drawn by the same code as the Today card. The curve builder moved out of `app/(tabs)/index.tsx` into `src/components/today-level-series.ts`. | The level curve is the category's proven feature and the thing a user pays for, so the paywall shows it instead of naming it in a bullet. Showing it meant a second caller for the curve, and the curve math was screen-local: `buildLevelSeries`, `levelWindowFrom`, `seriesBetween` and `doseIn` lived inside the Today screen. Copying them would have given the app two curves that drift apart on the first edit, and the curve is the one picture the user trusts, so the four functions moved to a shared module that both screens read. The module samples `src/domain/pk.ts` and decides nothing, so the domain is untouched. The paywall passes `nextDoseAt: null`, because dose state and scheduling are Today's job and the next dose only sets the right edge: the estimate sums logged shots alone. The chart runs in free mode, shape without a number, exactly as Today draws it for a user without Pro, and with no unlock chip, because the chip's only job is to open this screen. A user with no logged shot gets the onboarding half-life drawing instead of a curve. Poke never draws a curve out of numbers nobody entered. | Live |
| 23 | Poke reads body mass from Apple Health on request, one way. The offer sits on the onboarding weight screen and on a Profile row that opens a sheet. No other HealthKit type, and Poke asks for no write permission at all. | A user whose scale writes to Apple Health has already weighed themselves, and the weight screen was asking them to type the number a second time. Body mass alone, because that is the number the request named and because every extra type adds another checkbox to the permission sheet the user has to read. Row 12 still holds: the Health database is on the same phone, the read is one way, and nothing leaves the device. The privacy screen's first promise said "Poke asks for no health data from another app and sends none to one", which stopped being true the day this shipped, so it now says "Poke sends your health data nowhere" — the half the read leaves standing, and it no longer names Apple on a screen that also ships to Android. The ask sits on the weight screen and not on that privacy screen ten steps later, because a permission reads as reasonable next to the work it saves, and by the privacy screen the user has already typed the number. Profile gets a row and a sheet, not a switch: iOS does not let an app revoke its own HealthKit read permission, so a switch would promise an undo Poke cannot perform, and the sheet sends the user to the Health app under Sharing instead. iOS also never reveals read authorization status, so a refused permission and an empty Health database are the same answer; every outcome message says what Poke did or did not get rather than guessing which happened, and `health_sync_enabled` is written only after a row actually arrives, so the Profile row cannot read "On" over a refusal. Imported rows are stored in kilograms whatever unit the user reads, keyed by the sample uuid against the unique partial index on `(source, source_id)`, so a re-read adds nothing and no HealthKit anchor is stored. `health_synced_at` is a display value that the importer only uses to open a read window a month wide behind itself; losing it costs one slow read of the whole history and no wrong rows. The native module is imported lazily inside the call, following `purchases.ts`, because Nitro binds at module scope and the web bundle has nothing to bind to. Reading a sample is pure in `src/services/healthWeight.ts` with 13 tests, and `src/services/health.ts` owns the native call and the database. Two things live outside this repo and have to follow: the App Store privacy nutrition label and the privacy policy at peptide.industries/privacy both have to say that health data is read. | Live |
| 24 | A logged shot is editable, on the screen that logged it. `/log-shot` with an `injectionId` reads the row, fills the form and saves through `updateInjection`. Every field moves, the medication included, and changing the medication does not reset the dose or the unit. | History could delete a shot and nothing else, so a shot filed on the wrong day or under the wrong medication had to be deleted and typed again from memory. One screen does both jobs, the way `app/medications/new.tsx` is also the medication editor: every field an edit changes is a field the log screen already asks for, and a second screen would be the same form with its own bugs. Owner's instruction, 13 Aug 2026: "let them edit the whole thing and make it fit why do we need to reset the unit etc?" So a medication change keeps what the user typed. A dose of 0.5 mg does not stop being 0.5 mg because the shot moved to another medication, and the level curve already reads a shot in the unit it was logged in and converts it (`doseIn` in `src/components/today-level-series.ts`), so a kept unit costs the chart nothing. Two things follow the new medication, and only two. The route, because no screen has ever asked a user for one, save has always copied it from the medication, and the diagram draws the sites of the route it is handed. And through the route the site, but only when the new route does not offer the site already on the shot: an abdomen is not a place an intramuscular needle goes. A site both routes offer stays exactly where the user put it, and a site that clears says why on the card rather than emptying in silence. That rule is `siteOnRoute` in `src/domain/shotEdit.ts`, tested against the catalogue itself so a site that changes routes moves the test with it. `created_at` stays out of `UpdateInjection`: it is when Poke wrote the row, and `taken_at` already carries when the shot happened. An edit hands Today no focus medication and asks for no App Store rating, because both belong to a shot the user just gave, and it returns to History rather than to Today. The History day sheet grew the second verb: a shot row carries a chevron, opens the editor on a tap, reads as one button to a screen reader, and keeps delete behind the swipe and on that button's accessibility actions. The row is one accessibility element and exactly one node may claim it, because a row that marks the swipe surface and the pressable inside it draws a button within a button, which is invalid on the web and reads the row out twice everywhere else. Two live bugs came out with the feature. Delete never invalidated the reminder queue, so a deleted shot left its check-in banner queued and never gave the day back its shot-day reminder; every write to the injections table now goes through `src/services/injectionMutations.ts`, which owns the `refreshScheduledReminders` call for create, update and delete alike. And Today celebrated any shot that appeared on today, so an edit that moved Tuesday's shot onto today played the log sequence and nudged the streak over a correction; the screen now remembers every shot id it has loaded rather than today's alone, so a shot already on file cannot read as new (`hasNewTodayShot`, same module, 16 tests in `src/domain/shotEdit.test.ts`). | Live |
| 25 | Every dot on the body diagram is reachable: one press surface over the whole diagram resolves a tap to the nearest dot, and the per-site elements stay for a screen reader alone. The glutes take a subcutaneous shot as well as an intramuscular one, and the route reads as "under the skin" and "into the muscle" everywhere a user sees it. | A beta user reported it on 13 Aug 2026: "Also not allowing me to choose glutes. You hit rear but it stays on upper left or something." Three separate faults made that one sentence. First, the glutes were an intramuscular-only row and every preset ships `defaultRoute: 'sc'`, so on a GLP-1 the diagram drew no glute at all and the tap landed on nothing. The buttock is a recognised subcutaneous site in standard injection teaching, so `routes: ['sc', 'im']` is the honest catalogue entry. Second, the tap targets were 44 pt boxes centred on each dot, and on a 119 pt diagram those boxes have to overlap, so the touch went to whichever box React rendered last rather than to the dot under the finger. The boxes now take no pointer at all and a single `Pressable` over the diagram measures the finger against every visible dot and picks the nearest, which is the rule a user already believes (`nearestDot` in `src/components/body-diagram-geometry.ts`, 12 tests). The per-site elements stay behind it, `pointerEvents="none"`, because a screen reader has to step through the sites one by one and a single surface offers it nothing; each one is centred on its dot, so a platform that turns an accessibility activation into a centre tap still resolves to the same site. They also carry `aria-checked` beside `accessibilityState`, because react-native-web drops the React Native prop and the web build is the verification surface. Third, the toggle keeps the selection when the body turns around, which is right, but the marked dot goes off screen with it, so the caption read "Upper left abdomen" over an empty back view and looked like a tap that did nothing. The caption now says "Upper left abdomen, on the front". Two things follow from making the glutes subcutaneous. The rotation would have proposed one for a GLP-1, and no Ozempic, Wegovy or Mounjaro label describes a shot there, so `suggestRoutes` on the catalogue row separates the site Poke offers from the site Poke puts forward, and a site the user has actually used rejoins the rotation, because a site in use and out of the rotation is the one way that rule could send somebody back to the same spot (`src/domain/rotation.ts`, 8 tests). And the line under the diagram said "Poke suggests the next site" over a site the user had just picked by hand, which is the same complaint in words, so it now says "Poke logs the site you picked". Owner's instruction on the wording, 13 Aug 2026: "SC and IM is hard for people to understand." `ROUTE_LABELS` already held a plain string beside the clinical one, and the medication list was the last screen still printing the abbreviation; `routeInLine` in `src/domain/peptides.ts` reads the same two strings the choice cards read, so a screen that names the route in a sentence cannot drift from a screen that names it on a card. | Live |
| 26 | An app update never touches the user's rows. The database file survives the install, migrations are append-only and run one statement at a time inside a transaction with the version write, `SCHEMA_VERSION` is read off the end of the array, a replay of migrations 1 to 8 may skip a column it already added, and a database that still cannot open offers a rescue export before it offers a retry. | Owner's question, 13 Aug 2026: "on app version updates let's make sure the user data preserves right?" The store install replaces the app bundle and leaves the app's Documents directory alone, so `peptide_tracker.db` survives on its own; everything that can lose data is in the upgrade Poke runs on the next launch. Four faults were there. `SCHEMA_VERSION` was a hand-typed constant sitting at 11 while `MIGRATIONS` held 12, so the number every other reader trusted was one behind the schema; it is now derived from the last entry and a test holds the array to one strictly increasing run. The runner ran a whole migration through `execAsync` as one string, which meant a statement-level repair was impossible. Migrations 1 to 8 shipped in builds 1.0.1 through 1.2.2 with no transaction at all, so a phone killed between the `ALTER TABLE` and the version write keeps the column, loses the version, replays the migration on the next launch and dies on `duplicate column name` — every launch, forever, with the data on the disk and unreachable. `LAST_UNTRANSACTED_VERSION = 8` is the repair, and it is deliberately narrow: the version must be one of those eight, the statement must be an `ALTER TABLE ... ADD COLUMN`, and SQLite must have said `duplicate column name`. A duplicate column in migration 9 or above is an authoring mistake and stays loud, and every other error still rolls the transaction back. And a launch that failed showed the user an error with a retry, which replays the same failing migration, so the screen now offers the export first: `exportWithoutMigrating` opens the same file on a second connection with `PRAGMA query_only = ON`, reads whatever schema it meets table by table, and shares a CSV. A rescue must not be able to write to a database Poke has already failed to upgrade, and it must not wait on the connection that failed. The runner moved to `src/db/migrate.ts` behind a `MigrationDriver` with no expo-sqlite in it, so `migrations.test.ts` drives the real `MIGRATIONS` array against a plain `node:sqlite` database: 16 tests, including an upgrade from every historical version to the newest with rows in the tables, a kill between the two halves of an untransacted migration, and the statement splitter against string literals, comments and a `CREATE TRIGGER` it refuses by name rather than cutting in half. | Live |
| 27 | The body diagram's target grows by drawing a bigger dot on a bigger figure, not by widening the tap radius. `DOT_RADIUS_VB` goes 3.6 → 4.4, and the log screen sizes the diagram from the window height (`width = clamp(110, 155, 17% of height)`, `height = 2 × width`) with no cap on the card. The save button leaves the scroll for a sticky footer. | Owner's instruction, 14 Aug 2026: "can we make the tap targets bigger?" The tap target had not been a 44 pt box since row 25: one press surface covers the whole diagram and hands the tap to the nearest dot inside 24 viewBox units, so every dot already owns its share of the picture and the torso and thighs are covered edge to edge. Raising `TAP_RADIUS_VB` would have changed nothing a finger can feel. What was small was the part of the target the eye can see. The dot was drawn 8.6 pt across on an iPhone 16 Pro, which asks for the careful, slow tap the geometry does not need, and a user who misses reads the miss as the app ignoring the tap — the same complaint row 25 answered. So the two real levers are the drawn scale and the dot radius. Scale is strictly `height ÷ 200`: the figure is 1:2, the SVG letterboxes rather than stretches (`xMidYMid meet`), and the card is about 322 pt wide, so a wider box only adds margin. Height is the whole lever, and the old sizing spent it backwards — it capped the card first (`min(390, 46% of height)`), took the diagram out of what was left, and capped the width at 190 pt, a number a height-bound diagram could never reach. That cap also clipped: under about 665 pt of window the 170 pt diagram floor plus the card's own 152 pt of chrome exceeded the cap, so the card cut off its own caption on a small phone. Width now follows height and the card is as tall as its contents. The radius ceiling is the closest pair the catalogue ever draws on one view, which is 12 units between the outer and the anterior thigh on the front of an intramuscular shot — not the 16 the old comment claimed, because that comment counted the belly pairs and missed the legs. 4.4 leaves 3.2 units of daylight between two dots, and the test derives the pair from `bodySites` rather than writing the number down, so a site that moves fails here instead of shipping one dot on top of another. The selected dot's three rings follow the same ceiling, and the screen reader's element is `max(44, dot)` — those overlap on a thigh, which decides nothing, because that layer takes no touch. Measured on an iPhone 16 Pro: the diagram 119x238 → 149x298, a dot 8.6 → 13.1 pt across, and the two upper abdomen dots 19 → 24 pt apart. The cost is honest and paid on purpose. The screen had about 11 pt of slack, so the extra height comes out of the fold, and the save moved to a footer under the scroll on the paywall's pattern rather than sitting at the end of it. At rest the caption's second line and the Details row now need one short swipe. A save the user has to go looking for is the worse loss: it is the one press the screen exists for. 15 tests in `src/components/body-diagram-geometry.test.ts`, including the dot floor at both the real size and the small-phone clamp. | Live |
| 28 | The Today hero chart is drawn in the same seven days the week axis labels: `weekWindow(now)` is today minus 3 to today plus 3, and both the chart and `buildWeek` read `WEEK_LOOKBACK_DAYS` out of `src/components/today-level-series.ts`. The next-dose circle draws only when the next dose falls inside that week. The paywall keeps the six half-life window, and `levelWindowFrom` now takes the first dose from the convertible doses alone. | A tirzepatide user with a shot on 7 Aug 2026 and a shot on 14 Aug 2026 saw the 7 Aug spike sitting over the Tuesday column and asked why the graph was flat from Tuesday to Thursday. The graph was right and the two ranges under it were not: the curve's x came from `levelWindow`, up to six half-lives clamped to 21 days plus a forecast to the next dose, while the row of labels under it always drew today minus 3 to today plus 3. Nothing tied the two together, so on a 5-day half-life the curve carried a month and the labels carried a week, and every feature in the shape stood over the wrong day. A chart whose own axis contradicts it is worse than no chart, because the user reads the contradiction as the estimate being broken. One window now serves both, stated once, so they cannot drift on the next edit. The left edge is not a reset to zero: `estimatedLevelAt` sums every dose taken at or before the sampled time, the ones before `fromMs` included, so the curve enters the week at the true residual of everything logged before it and a shot three weeks back still shows in the height of the line. The next dose used to set the right edge, and with a fixed right edge a dose beyond the last column would have drawn its hollow circle pinned to the edge on a day that is not its day, so Today passes `nextDoseAt` only when it lands inside the week and passes null otherwise. The dashed forecast still runs to the end of the week either way, because the estimate falls out of shots already logged and needs no scheduled dose to continue. The paywall hero keeps `levelWindow`, because it has no day axis under it and the whole decay is the picture it sells. While the window was being split from the curve, `levelWindowFrom` was reading its first dose off the raw injection rows and `buildLevelSeries` was dropping rows whose unit does not convert, so one old IU shot on a medication dosed in mg opened the window weeks back over a flat zero the curve never draws. The conversion is now one exported step, `convertedDoses`, and the window and the curve read the same list. The chart's `PAD_X` reads `spacing.xl`, the padding the axis row already used, so a day column centre is the x of that day's midpoint on the curve rather than 20 by coincidence in two files. 9 tests in `src/components/today-level-series.test.ts`. | Live |
| 29 | A cycle is four columns on `medications` plus one preference, and pause is the primitive it is built on. No `cycles` table, no cycle history, no per-peptide default duration, and no number Poke did not receive from the user. | Peptide users run courses, not open-ended prescriptions: 8 weeks on BPC-157 then 4 weeks off is the shape of the category, and Poke had no word for it. The cheapest true model was already half built. `status = 'paused'` existed and stopped the reminders, so a break is a pause that knows when it started, and the whole feature is `cycle_days_on`, `cycle_days_off`, `cycle_started_at` and `paused_at` in migration 13, with `notif_cycle_enabled` beside the other three notification switches. A `cycles` table was rejected for v1: it buys a history of past courses that no screen asks for, and it costs a join on every read of the medication list, the Today hero and the reminder builder. It is the right answer the day Poke models a stack or a protocol with more than one on/off leg, and that is the trigger to revisit this row. The durations are always user input. The chip rows offer 4, 6, 8 and 12 weeks on and 2, 4, 6 and 8 weeks off because those are the numbers people already say to each other, but nothing is preselected and "Other" takes any count of days, so a saved plan is a number the user chose and not one Poke put in front of them. A per-peptide default was the obvious next step and is the one this row refuses: `store-setup.md` §1A keeps the app clear of advice, and a duration that arrives with the preset is a recommended course of treatment however quietly it is worded. The same rule shapes the notifications. Exactly two banners per cycle, the last planned day and the day the break ends, both stating what the user set and neither asking for anything: "The plan you set ends today" and "You set 4 weeks off". The last-day card offers "Start break" and "Keep going", and "Keep going" writes nothing at all, because passing the last day of a plan changes no fact about the medication. Tomorrow the state reads `pastPlan`, which counts on past the total ("Week 9 of 8") and never nags. Resume is the one write that moves time: `cycle_started_at` becomes today, so the next cycle counts from the resume and not from the install. That is also why the anchor split. `medicationScheduleFromStored` now takes `cycle_started_at ?? created_at`, and the forward-looking callers pass it (the reminder builder, the Today dashboard) while the backward-looking ones do not (`streaks.ts`, `historyBoard.ts`, the Progress adherence score), because moving the anchor under a record of what already happened would rewrite the user's history on every resume. State derivation is pure in `src/domain/cycle.ts` with 18 tests, and it says weeks only when the count divides by seven, so a 30 day plan reads "Day 12 of 30" rather than a fraction of a week nobody typed. | Live |
| 30 | The week axis under the Today hero is two elements per column, not three: a top label over one date cell that carries both the date and the state. The cell is a filled disc with a corner check when the shot is logged, a strong ring when the dose is due, a hollow ring when the schedule names the day and a bare number on a day off. Today's label is the word TODAY in the medication's colour, so the date lives in the cell alone. | User feedback of 14 August 2026 on the shipped row: the composite today label read "F 14" and nobody could parse it, and a letter over a date over a separate status mark put three things in a 50 pt column that the eye had to assemble before it meant anything. Merging the date into the mark removes a whole row from the card and gives each state a number to sit in, which is the thing the user was hunting for. The word TODAY replaces the letter because the one column that has to be found at a glance should say its name rather than share a letter with Thursday and Tuesday. The corner check keeps the logged state readable when the disc is any of the six medication hues, and its fill is the medication's own colour taken to about a third of its lightness rather than `successDeep`: a green check on a blue or a pink disc would read as a second meaning. The motion contract did not change. The arrival wipe still crosses the seven columns one beat apart and the log pop and the single pulse ring still land on today, all now aimed at the 24 pt cell, and the row keeps `paddingHorizontal: spacing.xl` so a column centre stays the x of that day on the curve. | Live |
| 31 | The weekly pace is editable after setup, on a Profile row under Goal weight, and that row is hidden when no goal weight is set | Pace 0 is Maintain, and before this an onboarded user could only reach it by redoing setup. The row sits under the goal because `services/onboarding.ts` writes `weekly_pace` only beside a goal weight, so a pace with no goal to apply it to is a slider position and not a plan. A row offered without a goal would take an answer Poke then throws away, so it is hidden rather than shown empty, and setting a goal on the row above brings it back. This differs from the goal row itself, which is always visible with a "Set a goal" pill, because a goal stands on its own. The sheet reuses the setup `Slider` and the shared `formatPace`, `formatPaceRate` and `paceBounds`, so setup and Profile cannot drift, and `convertPace` moved from private to exported for the same reason. The value is held in a draft and written once on the press, because the slider fires on every step and a write per step is a hundred UPDATE statements under one thumb. The unit toggle now converts `weekly_pace` the way it already converted the weights, since a saved 1 lb a week otherwise reads as 1 kg a week, which is more than twice the plan the user set. A pace stored before this change and then read under a switched unit cannot be corrected after the fact. Nothing in the app reads `weekly_pace` yet, so the sheet promises no date and no projection. | Live |
| 32 | The catalog carries prescription hormones: testosterone in four esters, estradiol in two, HCG, gonadorelin and kisspeptin, each ester as its own row with its own half-life | TRT users log injections on the same weekly cadence as GLP-1 users, and the beta feedback asked for more of the stack in one app. One row per ester, not one "Testosterone" row, because the esters differ in half-life by a factor of ten and a merged row would draw a wrong curve for most users. The rows record what the user was prescribed; Poke still proposes no dose and sells nothing, and the review notes say so. | Live |
| 33 | A blend preset has no half-life of its own. Its curve is the sum of its parts, and only when the user copies the milligrams of each part from the vial label into `medications.composition` | `pk.ts` is linear in dose, so superposition is exact within the model Poke already ships: each part receives `dose × (part mg / total mg)` and decays at its own sourced rate. No composition means no curve, because a default split would be a number Poke made up. The typed boxes save all or nothing (`compositionDraft`), because a half-copied label silently hands the missing part's milligrams to the typed ones. A part with no sourced half-life keeps its milligrams in the denominator and draws nothing, so the curve under-draws and never inflates. The chart window is sized by the slowest part, whose tail the chart has to hold. | Live |
| 34 | Onboarding accepts any number of custom medications, and the schedule flow is one screen per medication indexed by route param | The old flow held one custom draft in a single store field, so a second custom overwrote the first, which was the exact beta complaint. The drafts live in a keyed record and `/onboarding/schedule/[index]` walks them, so adding a fifth medication changes no screen code. | Live |
| 35 | One medication can carry a different dose per scheduled weekday, stored as a JSON map in `medications.dose_by_day` keyed by the `getDay` weekday, with `default_dose` as the fallback for unmapped days | A customer on 6 mg Monday and 2 mg Thursday would otherwise create Reta1 and Reta2, which splits one pharmacokinetic history across two curves. The level chart already reads logged doses, so the map only has to fix the plan-side surfaces: prefill, Today, reminders, the list line and the history planned row. The option shows only when the schedule names two or more weekdays, and `daily` is excluded on purpose because a seven-row dose sheet is a form nobody asked for. Readers go through `parseDoseByDay`, which reads anything malformed as null, so a damaged row degrades to the single-dose medication it would have been. | Live |
